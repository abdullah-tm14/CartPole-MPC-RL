# MPC

Place the MATLAB model predictive control files in this folder.

Keeping the MPC and RL implementations in separate folders makes their source files, dependencies, and results easy to distinguish.
